# Machine Vision by IRiSH — Layout & UI ปัจจุบัน (หลังตัดชั้นสอน)

> อธิบายหน้าตา/เลย์เอาต์ปัจจุบัน + ทุกปุ่มทำอะไร + ระบบที่มีในโปรแกรม
> เวอร์ชัน v0.1.0 · อัปเดต 2026-08-17
> **การเปลี่ยนแปลงล่าสุด:** ตัด Learning Mode + คำอธิบายเชิงสอนทั้งหมดออก · ชื่อเครื่องมือ/หมวด/วิธี เป็นภาษาอังกฤษ (label ปุ่มปรับค่ายังเป็นไทย)

---

## 0. ภาพรวม

แอปเดสก์ท็อป (Electron) ต่อ "กล่อง" เป็นสายแบบ Node-RED → ภาพวิ่งผ่านทีละกล่อง → เห็นผลสดทันที
เทคโนโลยี: Electron + OpenCV.js (Web Worker) + LiteGraph.js + MediaPipe Pose + onnxruntime-web (YOLO)
UI: โครง/สถานะเป็นไทย · **ชื่อเครื่องมือเป็นอังกฤษ**

```
แหล่งภาพ (กล้อง / ไฟล์ภาพ / เฟรมจากวิดีโอ)
   → กล่องประมวลผล (filter) → … → กล่องผลลัพธ์ (Display)
```

---

## 1. โครงหน้าจอ (Layout)

```
┌──────────────────────────────────────────────────────────────────────┐
│ TOPBAR: [โลโก้]   แหล่งภาพ │ นำเข้า  ····  ควบคุม │ ส่งออก & ตั้งค่า    │
├───────────┬──────────────────────────────────────┬───────────────────┤
│ PANEL     │  LIVE VIEW  (แถบหัว + ข้อมูลภาพ)      │ คุณสมบัติ          │
│ LEFT      │  ── พื้นที่ภาพ (ใหญ่กว่า ~58%) ──     │ PROPERTIES        │
│ กล่อง     │                                      │ • ชื่อ+หมวด+วิธี   │
│ เครื่องมือ├──────────────────────────────────────┤ • พารามิเตอร์     │
│ (English) │  CAPTURE TRAY (ถ้ามีภาพแคป)          │                   │
│           ├──────────────────────────────────────┤ ─────────         │
│           │  PIPELINE CANVAS (~42%)              │ PERFORMANCE       │
│           │                                      │ LOGS (พับได้)     │
├───────────┴──────────────────────────────────────┴───────────────────┤
│ STATUSBAR: สถานะซ้าย          · จุดสุขภาพ · เวอร์ชัน ขวา              │
└──────────────────────────────────────────────────────────────────────┘
```

- 3 คอลัมน์: ซ้าย 260px · กลาง ยืด · ขวา 320px
- กลางแบ่งบน/ล่าง: **Live View ได้พื้นที่มากกว่า Pipeline** (~58/42) และ Live View มี floor สูงกว่า → เป็นพระเอกเสมอ
- **Live View** = แถบหัวจริง (ไม่ลอยทับภาพ) + พื้นที่ภาพด้านล่าง (`.stage-body` ครอบทุก overlay ให้อยู่ใต้หัว)

**หน้า Loading:** โลโก้ IRiSH + spinner + ข้อความตามบริบท ("กำลังโหลด OpenCV.js…" → "กำลังเปิดกล้อง…") บล็อกจนพร้อม

> ⚠️ ไม่มี Onboarding เด้งครั้งแรกแล้ว · ไม่มีปุ่ม ? · ไม่มีแถบ Tips

---

## 2. TOPBAR — แถบบนสุด

โครง: `[แหล่งภาพ] │ [นำเข้า] ····spacer···· [ควบคุม] │ [ส่งออก & ตั้งค่า]`
แต่ละคลัสเตอร์มี **ป้ายกำกับเล็ก** (caption) อยู่บนปุ่ม

### 2.1 คลัสเตอร์ "แหล่งภาพ"
| ปุ่ม | หน้าที่ | เงื่อนไข |
|---|---|---|
| **📷 กล้อง** | ใช้ภาพสดจาก webcam | เสมอ (segmented) |
| **🖼️ ภาพนิ่ง** | ใช้ภาพ/วิดีโอจากไฟล์ | เสมอ (segmented) |
| **📸 แคปภาพ** | เก็บเฟรมกล้องเข้าคลัง (ถาดล่าง) | โหมดกล้อง · มี keycap ของคีย์ลัด |
| **ต้นฉบับ** | ภาพกล้องผ่าน filter บนกระดาน | โหมดกล้อง |
| **🧍 Ergonomics Pose** | จับโครงร่างกาย + วิเคราะห์ท่าทาง (ไม่ผ่านกระดาน) | โหมดกล้อง |
| **กล้อง: [เลือก]** | เลือกกล้อง (Port) | โหมดกล้อง + มีกล้อง > 1 |

### 2.2 คลัสเตอร์ "นำเข้า"
| ปุ่ม | หน้าที่ |
|---|---|
| **＋ นำเข้าภาพ / วิดีโอ…** | เปิดไฟล์ภาพ/วิดีโอ (วิดีโอ → เล่นก่อนแล้วแยกเฟรม) — เด่นเมื่ออยู่โหมดภาพนิ่ง |
| **📁 นำเข้าโฟลเดอร์** | นำเข้าหลายภาพพร้อมกัน (ทำ dataset) |
| **◀ ภาพ X/N ▶** | เลื่อนดูทีละภาพในชุด (แสดงเมื่อมีภาพ > 1) |

### 2.3 คลัสเตอร์ "ควบคุม" (โผล่เฉพาะโหมดกล้อง)
| ปุ่ม | หน้าที่ |
|---|---|
| **⏸ หยุด / ▶ รัน** | หยุด-เริ่มประมวลผลสด (ประหยัด CPU) |

### 2.4 คลัสเตอร์ "ส่งออก & ตั้งค่า"
| ปุ่ม | หน้าที่ | สี |
|---|---|---|
| **💾 Save Picture** | เซฟภาพผลลัพธ์ทุกกล่องเป็น PNG (เลือกโฟลเดอร์) | เขียว |
| **⬇ Export Yolo** | สร้าง/ดาวน์โหลดโค้ดเทรน YOLO (.ipynb) | ม่วง ghost |
| **ความละเอียด: [เลือก]** | ย่อภาพก่อนประมวลผล ลดหน่วง | select |
| **⚙** | ตั้งค่าคีย์ลัด (keycap) | icon |

> โค้ดที่ Export YOLO สร้าง = ล็อกไว้ ห้ามแก้

---

## 3. PANEL ซ้าย — "กล่องเครื่องมือ"

เรียงบนลงล่าง:
1. หัวข้อ "กล่องเครื่องมือ" + คำใบ้สั้น (แหล่งภาพ → ประมวลผล → ผลลัพธ์)
2. **🔍 ค้นหาเครื่องมือ** + keycap `Ctrl /` — พิมพ์แล้วซ่อน/โชว์ปุ่มทันที หมวดที่ไม่มีผลลัพธ์ถูกซ่อน
3. **📋 เริ่มจากเทมเพลต** — เปิดแกลเลอรีเทมเพลต
4. **🖥️ + เพิ่มกล่องผลลัพธ์** — เพิ่มจอผลลัพธ์อีกจอ
5. **หมวด Deep Learning** → **🧠 + เพิ่มกล่อง YOLO Detect**
6. **7 หมวดเครื่องมือ** (ชื่ออังกฤษ + สีประจำหมวด):

| หมวด (English) | สี | เครื่องมือในหมวด |
|---|---|---|
| **Color Space** | Cyan | Grayscale · Color Space Convert · Channel Split |
| **Enhancement** | Green | Brightness / Contrast · Gamma Correction · Histogram Equalization · CLAHE · Sharpen (Unsharp Mask) · Laplacian Sharpening |
| **Restoration** | Orange | Salt & Pepper Noise · Median Blur · Gaussian Blur · Average (Box) Blur · Bilateral Blur |
| **Morphology** | Purple | Morphological Operation |
| **Segmentation** | Red/Pink | Binary/Otsu/Adaptive Threshold · Canny Edge · Sobel Edge · Find Contours · Connected Components · Watershed |
| **Object Detection** | Violet | Bounding Boxes · Hough Circles · GrabCut |
| **Colour** | Blue | HSV inRange · Invert |

**การ์ดเครื่องมือ:** `[icon chip สีหมวด] ชื่อเครื่องมือ` (1 บรรทัด, ไม่มี subtitle อธิบายแล้ว)
คลิก 1 ครั้ง = วาง node ลงกระดาน + เลือกอัตโนมัติ

> ตัดออกแล้ว: การ์ด Learning Mode + subtitle ใต้ชื่อเครื่องมือ

---

## 4. STAGE — พื้นที่ผลลัพธ์ (กลางบน)

### 4.1 แถบหัว LIVE VIEW (แถบจริง เหนือภาพ)
`LIVE VIEW  ● สด    แหล่ง กล้อง · ความละเอียด 1280×720 · FPS 62 · สี RGB    120 MB`
- ข้อมูลภาพ inline ชิดขวา (ไม่ทับภาพ) · อัปเดตทุก 1 วิ เขียนเฉพาะ text
- ไม่มีแหล่งภาพ → ซ่อนข้อมูล · ภาพนิ่ง → ซ่อนแถว FPS

### 4.2 ปุ่มลอย (ล่าง)
| ปุ่ม | หน้าที่ |
|---|---|
| **📸** | แคปภาพเข้าคลัง (โหมดกล้อง) |
| **⛶** | เต็มจอ |

### 4.3 จอผลลัพธ์แต่ละกล่อง (stage-cell)
- ชิปหัวจอ = ชื่อกล่อง (ตั้งเอง / `Display N`)
- **Breadcrumb:** `ภาพ › Gaussian Blur ▾ › Canny ▾` — ขั้นที่มีพารามิเตอร์มี `▾` (คลิกดูค่าดิบ + ✎ ไปแก้), hover เห็นสรุปค่า; ขั้นสูตรตายตัวไม่มี `▾`
- แคนวาสภาพ + บรรทัด 📊 (เช่น "พบวัตถุ 3 ชิ้น")

### 4.4 พื้นที่ภาพสลับตามโหมด (`.stage-body`)
| โหมด | element |
|---|---|
| ต้นฉบับ / ภาพนิ่ง | `stage-grid` (grid หลายจอ) |
| Ergonomics Pose | `pose-canvas` |
| YOLO Detect | `det-canvas` |
| วิดีโอ | `video-source` + 🎬 แยกเฟรม + ✕ ปิด |
| ภาพนิ่งยังไม่มีภาพ | empty state: 🖼️ + ปุ่ม ＋ เลือกภาพ/วิดีโอ + 📁 โฟลเดอร์ |
| กล้องพัง | error: ⚠ + ขั้นตอนแก้ + ปุ่ม [เลือกกล้องใหม่] [ใช้ภาพนิ่งแทน] |

---

## 5. CAPTURE TRAY — ถาดภาพแคป (โผล่เมื่อมีภาพ)

| ปุ่ม | หน้าที่ |
|---|---|
| **▶ ใช้ในกระดาน** | เอาภาพในคลังเป็นแหล่งภาพนิ่ง |
| **🧍 ดู Ergonomics** | วิเคราะห์ท่าทางจากภาพที่เลือก (modal) |
| **💾 เซฟทั้งหมด** | เซฟ PNG ทั้งชุด (เลือกโฟลเดอร์) |
| **ล้างคลัง** | ลบทั้งหมด |
| thumbnail | คลิก = เลือก (ขอบ accent) · ✕ = ลบทีละภาพ |

---

## 6. PIPELINE CANVAS — กระดานต่อกล่อง

- ป้าย `PIPELINE CANVAS` + คำใบ้ "ลากสายจากจุดวงกลมเพื่อเชื่อมการประมวลผล"
- พื้นมืด (`--bg-panel`) · เส้นเชื่อม 2px · node วาดเองบน canvas

**Node card** (วาดด้วย `onDrawForeground`):
```
● Restoration            ← แถบหัวมืด + จุดสีหมวด + ชื่อ (อังกฤษ)
 เข้า           ออก       ← จุดต่อสาย
 [💧] Gaussian Blur       ← icon + ชื่อวิธีที่เลือก
      ksize: 5           ← ค่าหลัก (ยังโชว์ — เป็นค่า ไม่ใช่คำอธิบาย)
```
ขนาด: filter/DL 220×92 · แหล่งภาพ 180×60 · ผลลัพธ์ 200×170 (มี preview)
States: hover (ขอบสีจาง) · selected (ขอบ 2px + glow เบา) · disabled (จาง 45%)

**ปุ่มควบคุมมุมมอง (มุมขวาล่าง):**
| ปุ่ม | หน้าที่ | คีย์ลัด |
|---|---|---|
| ↶ / ↷ | ย้อนกลับ / ทำซ้ำ | Ctrl+Z / Ctrl+Y |
| ＋ / − | ซูมเข้า/ออก | |
| ⤢ | จัดมุมมองให้พอดี | |
| 🗑 | ลบกล่องที่เลือก | Delete |

---

## 7. PANEL ขวา — PROPERTIES (คุณสมบัติ)

หัว `คุณสมบัติ · PROPERTIES` · เนื้อหาเปลี่ยนตามกล่องที่เลือก

### 7.1 เลือกกล่อง filter
- **ชื่อเครื่องมือ (อังกฤษ)** + ป้ายหมวด
- **▶ ชื่อวิธีที่เลือก** (กล่องรวมหลายวิธี)
- หมายเหตุอัตโนมัติ (ℹ️ แปลงขาวดำอัตโนมัติ / 📊 พบวัตถุ N ชิ้น)
- หัวข้อ **"พารามิเตอร์"** คั่น
- ตัวควบคุม:
  - `number` → **slider** + ค่าปัจจุบัน (ไม่มีคำอธิบาย/hint แล้ว)
  - `select` → **รายการปุ่มติ๊ก ✓** (ตัวเลือกเป็นชื่อวิธีอังกฤษ, ไม่มีคำอธิบายรายตัวเลือกแล้ว)
  - `boolean` → **toggle ON/OFF**
- ว่าง → "เลือกกล่องบนกระดานเพื่อปรับค่า"

> ตัดออกแล้ว: การ์ด 💡 หลักการ / 🧪 ลองทดลอง / 📌 ใช้เมื่อ · subtitle "ปรับค่าเครื่องมือ" · คำอธิบาย param

### 7.2 เลือกกล่องผลลัพธ์
ตั้งชื่อกล่อง · รายการขั้นตอนที่ภาพผ่าน (คลิกไป node) · **🐍 สร้างโค้ด Python (OpenCV)**

### 7.3 เลือกกล่อง YOLO Detect
สถานะโมเดล · **📂 เลือกโมเดล** (best.pt / yolo / .onnx)

### 7.4 ท้ายแผง (พับได้)
- **ประสิทธิภาพ · PERFORMANCE** — FPS · CPU · RAM (จริงจาก Electron) · GPU = `—` (ไม่มีค่าปลอม)
- **บันทึกการทำงาน · LOGS** — เก็บสถานะอัตโนมัติ (MutationObserver) · พับเป็นค่าเริ่มต้น · auto-expand เมื่อเจอ error · ปุ่ม "ล้าง"

---

## 8. STATUSBAR — แถบล่าง
ซ้าย = ข้อความสถานะล่าสุด · ขวา = จุดสุขภาพ + `Machine Vision by IRiSH · v0.1.0`

---

## 9. Dialog ที่มี
| Dialog | เปิดจาก | หน้าที่ |
|---|---|---|
| เทมเพลต | 📋 แผงซ้าย | การ์ดเทมเพลต (ชื่อ · desc · ระดับ · 🎯 objective · ⏱ เวลา · [เริ่มทดลอง]) |
| โค้ด Python | 🐍 แผงขวา | โค้ด `cv2.*` ตามลำดับจริง + คัดลอก/ดาวน์โหลด |
| Export YOLO | ⬇ Export Yolo | ชุดข้อมูล + .ipynb (โค้ดล็อก) |
| แยกเฟรมวิดีโอ | 🎬 | สไลเดอร์ความถี่ + กริดพรีวิว |
| ตั้งค่าคีย์ลัด | ⚙ | อัดคีย์ผูก action |
| Ergonomics | 🧍 ถาดภาพ | ภาพ + โครงกระดูก + ผลประเมิน |

> Onboarding ถูกตัดออกจากการเด้งครั้งแรก (โค้ดยังอยู่แต่ไม่ถูกเรียก)

---

## 10. คีย์ลัด
| คีย์ | ทำอะไร | ตั้งค่าได้ |
|---|---|---|
| Space | แคปภาพ | ✓ (⚙) |
| M | สลับ ต้นฉบับ ↔ Pose | ✓ |
| C | สลับ กล้อง ↔ ภาพนิ่ง | ✓ |
| ← / → | ภาพก่อนหน้า / ถัดไป | ✓ |
| E | โค้ด YOLO | ✓ |
| Ctrl+Z / Ctrl+Y | ย้อนกลับ / ทำซ้ำ | ตายตัว |
| Delete | ลบกล่อง | ตายตัว |
| Ctrl+/ | โฟกัสช่องค้นหา | ตายตัว |
| Esc | ปิด dialog (ปุ่ม data-close) | ตายตัว |

กติกา: ไม่จับคีย์ลัดขณะพิมพ์ใน input / ขณะมี dialog เปิด (ยกเว้น Esc)

---

## 11. ระบบที่มีในโปรแกรม (สรุป)

| ระบบ | สถานะ | ไฟล์ |
|---|---|---|
| Design Token (dark industrial) | ใช้งาน | `styles/app.css` |
| Layout 3 คอลัมน์ + Live View priority | ใช้งาน | `index.html`, `app.css` |
| Topbar cluster + caption | ใช้งาน | `ui/source-bar.js` |
| Tool Palette + ค้นหา (Ctrl+/) | ใช้งาน | `ui/palette-panel.js` |
| Live View + ข้อมูลภาพ inline | ใช้งาน | `index.html`, `app.js` |
| Node Card (วาดบน canvas + ค่าหลัก) | ใช้งาน | `graph/editor.js` |
| Properties 3 โหมด + toggle/params-head | ใช้งาน | `ui/detail-panel.js` |
| Template (การ์ด objective/เวลา) | ใช้งาน | `app.js`, `ui/onboarding.js` |
| Breadcrumb / Pipeline trace | ใช้งาน | `app.js` |
| Capture Tray + Ergonomics | ใช้งาน | `app.js`, `mediapipe-pose.js` |
| Performance / Logs (พับได้) | ใช้งาน | `app.js`, `electron/main.js` |
| State feedback (empty/error/loading) | ใช้งาน | `index.html`, `app.js` |
| Keymap + Esc/Ctrl+/ | ใช้งาน | `app.js`, `ui/settings-dialog.js` |
| Undo/Redo (graph) | ใช้งาน | `graph/editor.js` |
| Codegen Python (OpenCV) | ใช้งาน | `codegen.js`, `ui/code-dialog.js` |
| Export YOLO (.ipynb) | ใช้งาน (ล็อก) | `ui/export-dialog.js` |
| Video → frames | ใช้งาน | `ui/video-frames-dialog.js` |
| **Learning Mode** | **ตัดออก** | — |
| **Learning card / คำอธิบาย** | **ตัดออก** | — |
| **Tool subtitle / param desc-hint** | **ตัดออก** | — |
| **Onboarding เด้ง / Tips** | **ตัดออก** | — |

---

## 12. หมายเหตุทางเทคนิค

- **ชื่ออังกฤษ:** เก็บใน `filters/registry.js` (`OPS[].name`, group strings, `GROUP_COLORS`/`GROUP_ORDER`). ชื่อเป็น display อย่างเดียว — ไม่กระทบ `apply`/schema
- **param label ยังเป็นไทย** (เช่น "ขนาด Kernel", "เลือกวิธี") ตามที่กำหนด
- **CSS ที่ค้าง** (`.learn-*`, `.param-desc`, `.choice-hint`, `.props-tips`, `.detail-panel-sub`) ยังอยู่ในไฟล์แต่ไม่มี element ใช้ — ไม่มีผลต่อการทำงาน (เก็บกวาดภายหลังได้)
- **ui-meta.js** ยังมี `OP_EXPERIMENT`/`OP_USECASE`/`GROUP_SUBTITLE` แต่ไม่ถูก import แล้ว · `GROUP_ICON`/`OP_PRIMARY`/helper ยังใช้อยู่ (icon การ์ด + ค่าหลักบน node)
- **ไม่แตะ backend:** OpenCV worker · LiteGraph execution · YOLO · MediaPipe · Export YOLO · Python generator · keymap logic → output pipeline เท่าเดิม
